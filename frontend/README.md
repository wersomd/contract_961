# 961.kz - Платформа подписания документов по SMS

## 📋 Обзор проекта

**961.kz** — это B2B-сервис для подписания документов через SMS-код. Платформа позволяет менеджерам компаний создавать заявки на подписание, после чего клиенты получают ссылки, просматривают документы и подписывают их с помощью SMS-кода.

### Технологический стек Frontend
- **React 18** + TypeScript
- **Tailwind CSS v4** (дизайн-система)
- **shadcn/ui** компоненты
- **React Router** для навигации
- **date-fns** для работы с датами
- **Sonner** для toast-уведомлений
- **Lucide React** для иконок

---

## 👥 Роли пользователей

### 1. **Admin** (Администратор)
- Полный доступ ко всем функциям
- Управление пользователями
- Настройки системы
- Просмотр аудит-логов

### 2. **Manager** (Менеджер)
- Создание заявок на подписание
- Управление клиентами
- Просмотр статистики
- Работа с документами

### 3. **Client** (Клиент)
- Просмотр документа по ссылке
- Подписание через SMS-код
- Скачивание подписанного документа

---

## 🎯 Основные модули

### 1. **Авторизация**
### 2. **Dashboard (Главная страница)**
### 3. **Заявки на подписание**
### 4. **Клиенты**
### 5. **Документы**
### 6. **Настройки**
### 7. **Журнал аудита**
### 8. **Клиентская часть (подписание)**

---

## 📦 Детальное описание фич

## 1. 🔐 **Авторизация**

### Страница: `/login`

#### Функционал:
- ✅ Вход по email + пароль
- ✅ Показ/скрытие пароля
- ✅ Восстановление пароля (кнопка, функционал нужен)
- ✅ Валидация полей
- ✅ Обработка ошибок

#### API Endpoints:
```
POST /api/auth/login
Body: {
  "email": "admin@961.kz",
  "password": "string"
}
Response: {
  "token": "jwt_token",
  "user": {
    "id": "string",
    "name": "string",
    "email": "string",
    "role": "Admin" | "Manager"
  }
}

POST /api/auth/logout
Headers: { Authorization: "Bearer {token}" }

POST /api/auth/forgot-password
Body: {
  "email": "string"
}
```

---

## 2. 📊 **Dashboard**

### Страница: `/dashboard`

#### Виджеты (статистика):
1. **Всего заявок** - общее количество
2. **На подписании** - заявки со статусом "pending"
3. **Подписано** - заявки со статусом "signed"
4. **Средний срок** - среднее время от создания до подписания

#### Таблица активных заявок:
- ID заявки
- Клиент (имя)
- Документ (название)
- Статус (badge)
- Дедлайн (дата)
- Кнопка "Открыть" → переход на `/requests/{id}`

#### Клик по заявке:
- Открывается детальная страница заявки

#### API Endpoints:
```
GET /api/dashboard/stats
Response: {
  "totalRequests": number,
  "pending": number,
  "signed": number,
  "averageTime": number // в часах
}

GET /api/dashboard/active-requests
Query params: ?limit=10
Response: {
  "requests": [SignRequest]
}
```

---

## 3. 📝 **Заявки на подписание**

### Страницы:
- `/requests` - список всех заявок
- `/requests/create` - wizard создания заявки
- `/requests/{id}` - детали заявки

### 3.1. Список заявок (`/requests`)

#### Функционал:
- ✅ Таблица всех заявок
- ✅ Поиск по: имени клиента, документу, ID заявки
- ✅ Фильтр по статусу (все, pending, viewed, signed, expired, canceled)
- ✅ Сортировка по дате создания
- ✅ Кнопка "Создать заявку"

#### Колонки таблицы:
- ID заявки
- Клиент (имя + телефон)
- Документ (название)
- Статус (badge с цветами)
- Создано (дата)
- Дедлайн (дата)
- Менеджер (кто создал)

#### Статусы заявок:
```typescript
type RequestStatus = 
  | 'pending'    // Ожидает подписания (желтый)
  | 'viewed'     // Просмотрено (синий)
  | 'signed'     // Подписано (зеленый)
  | 'expired'    // Истек срок (серый)
  | 'canceled';  // Отменено (красный)
```

#### API Endpoints:
```
GET /api/requests
Query params:
  ?search=string
  &status=pending|viewed|signed|expired|canceled
  &page=1
  &limit=20
Response: {
  "requests": [SignRequest],
  "total": number,
  "page": number,
  "totalPages": number
}
```

---

### 3.2. Создание заявки (`/requests/create`)

#### Wizard - 2 шага:

#### **ШАГ 1: Выбор/создание клиента**
- **Select клиента** из базы:
  - Опция "Новый клиент" (ручной ввод)
  - Список существующих клиентов: "Имя • Телефон"
  - При выборе → автозаполнение полей
- **Кнопка "+" (Добавить клиента)** → открывает диалог:
  - ФИО клиента (обязательно)
  - Телефон (обязательно, 11 цифр)
  - Компания (опционально)
  - Сохранение → клиент добавляется в базу + автоматически выбирается
- **Поля при "Новый клиент"**:
  - ФИО клиента (обязательно)
  - Телефон (обязательно, валидация 11 цифр)
  - Комментарий (опционально)

**Валидация телефона**:
- Должно быть ровно 11 цифр (любой формат ввода)
- Примеры валидных: `+77070000102`, `8 707 000 0102`, `77070000102`

#### **ШАГ 2: Загрузка документа**
- Название документа (обязательно)
- Загрузка файла (обязательно, поддержка PDF)

#### После создания:
1. Заявка сохраняется в базе со статусом `pending`
2. Генерируется уникальная ссылка: `https://961.kz/sign/{token}`
3. Отправляется SMS клиенту с текстом: `"Подпишите документ: {link}"`
4. Перенаправление на `/requests/{id}`
5. Toast: "Заявка создана! SMS отправлено клиенту"

#### API Endpoints:
```
POST /api/requests
Body: {
  "clientId": "string" | null,  // если выбран существующий
  "clientName": "string",        // если новый клиент
  "clientPhone": "string",       // формат: +77070000102
  "comment": "string | null",
  "documentName": "string",
  "documentFile": File           // multipart/form-data
}
Response: {
  "request": SignRequest,
  "signLink": "https://961.kz/sign/{token}"
}

// Если клиент новый и нужно сохранить в базу
POST /api/clients
Body: {
  "name": "string",
  "phone": "string",
  "company": "string | null"
}
Response: {
  "client": Client
}
```

---

### 3.3. Детали заявки (`/requests/{id}`)

#### Информация:
- **Статус** (большой badge сверху)
- **Timeline событий**:
  - Создано (дата/время, менеджер)
  - Отправлено (дата/время)
  - Просмотрено (дата/время, IP клиента) - если viewed/signed
  - Код отправлен (дата/время) - если signed
  - Подписано (дата/время, IP клиента) - если signed
  - Отменено (дата/время, причина) - если canceled
  - Истек срок (дата/время) - если expired

#### Информация о клиенте:
- Имя
- Телефон (font-mono)

#### Детали заявки:
- Дата создания
- Дедлайн
- Менеджер (кто создал)

#### Документ:
- Название
- **Просмотр PDF** (встроенный viewer)
- Кнопка "Скачать PDF"

#### Действия (кнопки):
- **Отправить повторно** (если pending/viewed) → отправить SMS снова
- **Отменить заявку** (если pending/viewed) → статус = canceled
- **Скачать подписанный** (если signed) → скачать PDF с подписью

#### API Endpoints:
```
GET /api/requests/{id}
Response: {
  "request": SignRequest,
  "timeline": [TimelineEvent],
  "documentUrl": "string",  // URL для просмотра PDF
  "signedDocumentUrl": "string | null"  // если подписано
}

POST /api/requests/{id}/resend
Response: {
  "success": true,
  "message": "SMS отправлено повторно"
}

PUT /api/requests/{id}/cancel
Body: {
  "reason": "string | null"
}
Response: {
  "success": true
}

GET /api/requests/{id}/document
Response: PDF file (Content-Type: application/pdf)

GET /api/requests/{id}/signed-document
Response: PDF file with signature
```

---

## 4. 👥 **Клиенты**

### Страница: `/clients`

#### Функционал:
- ✅ Таблица всех клиентов
- ✅ Поиск по имени, телефону
- ✅ Кнопка "Добавить клиента" → диалог
- ✅ Клик по клиенту → детали (можно реализовать позже)

#### Колонки таблицы:
- Клиент (аватар + имя + ID)
- Телефон (font-mono)
- Компания
- Количество заявок
- Последняя заявка (дата)

#### Диалог добавления клиента:
- ФИО клиента (обязательно)
- Телефон (обязательно, 11 цифр)
- Компания (опционально)
- Кнопки: "Отмена" / "Добавить клиента"

#### API Endpoints:
```
GET /api/clients
Query params:
  ?search=string
  &page=1
  &limit=20
Response: {
  "clients": [Client],
  "total": number,
  "page": number,
  "totalPages": number
}

POST /api/clients
Body: {
  "name": "string",
  "phone": "string",
  "company": "string | null"
}
Response: {
  "client": Client
}

GET /api/clients/{id}
Response: {
  "client": Client,
  "requests": [SignRequest]  // история заявок клиента
}
```

---

## 5. 📄 **Документы**

### Страница: `/documents`

#### Функционал:
- ✅ Таблица всех загруженных документов
- ✅ Поиск по названию
- ✅ Фильтр по статусу (все, подписано, ожидает)
- ✅ Кнопка "Загрузить документ" (без привязки к заявке)

#### Колонки таблицы:
- Название документа
- Статус (подписано / ожидает)
- Связанная заявка (ID, клик → `/requests/{id}`)
- Дата загрузки
- Размер файла
- Действия: Просмотр / Скачать / Удалить

#### API Endpoints:
```
GET /api/documents
Query params:
  ?search=string
  &status=all|signed|pending
  &page=1
  &limit=20
Response: {
  "documents": [Document],
  "total": number
}

POST /api/documents
Body: multipart/form-data {
  "file": File,
  "name": "string"
}
Response: {
  "document": Document
}

GET /api/documents/{id}
Response: PDF file

DELETE /api/documents/{id}
Response: { "success": true }
```

---

## 6. ⚙️ **Настройки**

### Страница: `/settings`

#### Вкладки:

### 6.1. **SMS-провайдер**
- Выбор провайдера (select): SMSC.ru, SMS Aero, Twilio
- API ключ (input type="password")
- Имя отправителя (max 11 символов латиницей)
- Шаблон SMS с ссылкой: `"Подпишите документ: {link}"`
- Шаблон SMS с кодом: `"Код подтверждения: {code}"`
- Кнопка "Отправить тестовое SMS"

### 6.2. **Пользователи**
- Список пользователей (только просмотр)
- Аватар, имя, email, роль (Admin/Manager)
- **Без кнопки "Изменить"** (управление через SQL)
- Текст: "Пользователи создаются администратором через SQL команды"

### 6.3. **Безопасность**
- **IP whitelist** (toggle) - ограничение доступа по IP
- **Логирование действий** (toggle, по умолчанию включено)
- **Время хранения логов** (select): 30/90/180/365 дней

### 6.4. **Уведомления**
- **Email-уведомления** (toggle) - о новых подписаниях
- **Напоминания о дедлайнах** (toggle) - за 24 часа
- **Уведомления об ошибках** (toggle) - сбои SMS

#### Кнопка:
- "Сохранить изменения" → сохраняет все настройки

#### API Endpoints:
```
GET /api/settings
Response: {
  "sms": {
    "provider": "smsc" | "smsaero" | "twilio",
    "apiKey": "string",
    "senderName": "string",
    "linkTemplate": "string",
    "codeTemplate": "string"
  },
  "security": {
    "ipWhitelist": boolean,
    "auditLogging": boolean,
    "logRetentionDays": number
  },
  "notifications": {
    "emailNotifications": boolean,
    "deadlineReminders": boolean,
    "errorNotifications": boolean
  }
}

PUT /api/settings
Body: { ... } // same structure as GET
Response: { "success": true }

POST /api/settings/sms/test
Body: {
  "phone": "string"
}
Response: { "success": true, "message": "SMS отправлено" }

GET /api/users
Response: {
  "users": [{
    "id": "string",
    "name": "string",
    "email": "string",
    "role": "Admin" | "Manager"
  }]
}
```

---

## 7. 📋 **Журнал аудита**

### Страница: `/audit-log`

#### Функционал:
- ✅ Таблица всех действий пользователей
- ✅ Поиск по: пользователю, ресурсу
- ✅ Фильтр по типу действия:
  - Все действия
  - Создание
  - Изменение
  - Удаление
  - Авторизация
- ✅ Фильтр по дате (input type="date")
- ✅ Кнопка "Экспорт CSV"

#### Колонки таблицы:
- Временная метка (дата + время)
- Пользователь (имя)
- Действие (иконка + текст)
- Ресурс (ID заявки/клиента/документа)
- IP адрес
- Детали (описание)

#### Типы действий:
- Создание заявки
- Отправка SMS
- Отмена заявки
- Подписание документа
- Изменение настроек
- Добавление клиента
- Просмотр документа
- Продление дедлайна
- Вход в систему
- Выход из системы

#### API Endpoints:
```
GET /api/audit-logs
Query params:
  ?search=string
  &action=create|update|delete|auth
  &date=2026-02-06
  &page=1
  &limit=50
Response: {
  "logs": [AuditEvent],
  "total": number,
  "page": number,
  "totalPages": number
}

GET /api/audit-logs/export
Query params: (same as GET)
Response: CSV file
```

---

## 8. ✍️ **Клиентская часть (подписание)**

### Страницы:
- `/sign/{token}` - просмотр документа
- `/sign/{token}/verify` - ввод SMS-кода
- `/sign/{token}/success` - успешное подписание

### 8.1. Просмотр документа (`/sign/{token}`)

#### Функционал:
1. **Проверка токена** - валиден ли, не истек ли срок
2. **Отображение информации**:
   - Логотип 961.kz
   - Название документа
   - Текст: "Для вас подготовлен документ на подпись"
3. **Встроенный PDF viewer** (полноэкранный)
4. **Кнопка "Подписать документ"** → отправляет SMS с кодом
5. **Mobile-first дизайн**

#### После клика "Подписать":
1. Отправляется SMS с 6-значным кодом
2. Статус заявки → `viewed` (если был pending)
3. Перенаправление на `/sign/{token}/verify`

#### Обработка ошибок:
- Токен невалиден → "Ссылка недействительна"
- Истек дедлайн → "Срок подписания истек"
- Уже подписано → "Документ уже подписан"

### 8.2. Ввод SMS-кода (`/sign/{token}/verify`)

#### Функционал:
1. **Поле ввода 6-значного кода** (auto-focus)
2. **Кнопка "Подтвердить"**
3. **Кнопка "Отправить код повторно"** (доступна через 60 сек)
4. **Таймер обратного отсчета**: "Запросить новый код через 00:59"

#### После ввода кода:
1. **Валидация кода** на сервере
2. Если верно:
   - Статус заявки → `signed`
   - Сохранение метаданных (IP, время)
   - Генерация подписанного PDF
   - Перенаправление на `/sign/{token}/success`
3. Если неверно:
   - Ошибка: "Неверный код. Попробуйте еще раз"
   - 3 попытки, затем блокировка на 10 минут

### 8.3. Успешное подписание (`/sign/{token}/success`)

#### Функционал:
1. **Иконка успеха** ✓
2. **Текст**: "Документ успешно подписан!"
3. **Название документа**
4. **Кнопка "Скачать подписанный документ"**
5. **Дата и время подписания**

#### API Endpoints:
```
GET /api/sign/{token}
Response: {
  "valid": boolean,
  "request": SignRequest,
  "documentUrl": "string",
  "error": "string | null"  // если expired/invalid
}

POST /api/sign/{token}/send-code
Response: {
  "success": true,
  "expiresIn": 300  // секунд
}

POST /api/sign/{token}/verify
Body: {
  "code": "string"  // 6-значный код
}
Response: {
  "success": boolean,
  "error": "string | null",
  "attemptsLeft": number
}

GET /api/sign/{token}/signed-document
Response: PDF file with signature
```

---

## 📊 **Структуры данных (Models)**

### SignRequest
```typescript
interface SignRequest {
  id: string;                    // REQ-2026-001
  clientName: string;
  clientPhone: string;           // +77070000102
  documentName: string;
  status: RequestStatus;
  createdAt: Date;
  deadline: Date;
  manager: string;               // имя менеджера
  comment?: string;
  viewedAt?: Date;
  signedAt?: Date;
  signToken: string;             // уникальный токен для ссылки
  smsCode?: string;              // 6-значный код (временный)
  smsCodeExpiresAt?: Date;
  ipAddress?: string;            // IP клиента при подписании
  verificationAttempts?: number; // счетчик попыток ввода кода
}
```

### Client
```typescript
interface Client {
  id: string;                    // CL-001
  name: string;
  phone: string;                 // +77070000102
  company?: string;
  requestsCount: number;
  lastRequestDate?: Date;
  createdAt: Date;
}
```

### Document
```typescript
interface Document {
  id: string;
  name: string;
  fileUrl: string;               // путь к файлу
  fileSize: number;              // в байтах
  status: 'pending' | 'signed';
  requestId?: string;            // связанная заявка
  uploadedAt: Date;
  uploadedBy: string;            // user ID
}
```

### AuditEvent
```typescript
interface AuditEvent {
  id: string;
  timestamp: Date;
  userId: string;
  userName: string;
  action: string;
  resource: string;
  ip?: string;
  details?: string;
  metadata?: Record<string, any>;
}
```

### TimelineEvent
```typescript
interface TimelineEvent {
  id: string;
  type: 'created' | 'sent' | 'viewed' | 'code_sent' | 'signed' | 'expired' | 'canceled' | 'failed';
  title: string;
  description?: string;
  timestamp: Date;
  metadata?: Record<string, any>;
}
```

---

## 🔔 **SMS логика**

### Отправка SMS с ссылкой:
- **Когда**: При создании заявки
- **Кому**: clientPhone
- **Текст**: `"Подпишите документ: https://961.kz/sign/{token}"`
- **Провайдер**: SMSC.ru / SMS Aero / Twilio (настраивается)

### Отправка SMS с кодом:
- **Когда**: При клике "Подписать документ" на клиентской странице
- **Кому**: clientPhone
- **Текст**: `"Код подтверждения: 123456"`
- **Код**: 6-значный, случайный
- **Срок действия**: 5 минут
- **Повторная отправка**: через 60 секунд

### SMS Provider интеграция:
```typescript
interface SMSProvider {
  send(phone: string, message: string): Promise<{
    success: boolean;
    messageId?: string;
    error?: string;
  }>;
}
```

---

## 📄 **Генерация подписанного PDF (ВАЖНО!)**

### Концепция:
После успешного подписания документа, **бэкенд создает новый PDF файл**, который содержит:
1. **Оригинальные страницы документа** (все страницы исходного PDF)
2. **Дополнительная страница с информацией о подписи** (добавляется в конец)

### Содержимое страницы подписи:

```
┌─────────────────────────────────────────┐
│                                         │
│    ДОКУМЕНТ ПОДПИСАН ЭЛЕКТРОННО         │
│                                         │
│    Подписано: 06.02.2026 в 14:30       │
│    Кем: Иванов Иван Иванович            │
│    Телефон: +7 707 000 0102             │
│                                         │
│    ┌─────────────┐                      │
│    │             │                      │
│    │   QR КОД    │  ← Ссылка на файл   │
│    │             │                      │
│    └─────────────┘                      │
│                                         │
│    ID заявки: REQ-2026-001              │
│    Платформа: 961.kz                    │
│                                         │
└─────────────────────────────────────────┘
```

### Данные на странице подписи:

1. **Заголовок**: "ДОКУМЕНТ ПОДПИСАН ЭЛЕКТРОННО"
2. **Подписано**: Дата и время в формате `06.02.2026 в 14:30`
3. **Кем**: Полное имя клиента
4. **Телефон**: Номер телефона клиента (font-mono)
5. **QR код**: Ссылка на подписанный документ `https://961.kz/signed/{requestId}`
6. **ID заявки**: REQ-2026-001
7. **Платформа**: 961.kz (брендирование)

### Технические детали (для бэкенда):

#### Библиотеки для генерации PDF:
- **Node.js**: `pdf-lib`, `pdfkit`, `hummus`
- **Python**: `PyPDF2`, `reportlab`, `fpdf`
- **Java**: `Apache PDFBox`, `iText`
- **Go**: `gofpdf`, `unidoc`

#### QR код:
- **Содержимое**: `https://961.kz/signed/{requestId}` или прямая ссылка на CDN
- **Размер**: 150x150px
- **Библиотеки**: `qrcode` (npm), `qrcode` (python), `go-qrcode`

#### Процесс генерации:

```typescript
// Псевдокод
async function generateSignedPDF(originalPDF: Buffer, signData: SignatureData): Promise<Buffer> {
  // 1. Загрузить ориг��нальный PDF
  const pdfDoc = await PDFDocument.load(originalPDF);
  
  // 2. Создать новую страницу
  const signaturePage = pdfDoc.addPage([595, 842]); // A4
  
  // 3. Добавить текст
  signaturePage.drawText('ДОКУМЕНТ ПОДПИСАН ЭЛЕКТРОННО', {
    x: 50,
    y: 750,
    size: 20,
    font: await pdfDoc.embedFont(StandardFonts.HelveticaBold),
  });
  
  signaturePage.drawText(`Подписано: ${formatDate(signData.signedAt)}`, {
    x: 50,
    y: 700,
    size: 12,
  });
  
  signaturePage.drawText(`Кем: ${signData.clientName}`, {
    x: 50,
    y: 680,
    size: 12,
  });
  
  signaturePage.drawText(`Телефон: ${signData.clientPhone}`, {
    x: 50,
    y: 660,
    size: 12,
  });
  
  // 4. Генерировать QR код
  const qrCodeUrl = `https://961.kz/signed/${signData.requestId}`;
  const qrCodeImage = await generateQRCode(qrCodeUrl);
  const qrImage = await pdfDoc.embedPng(qrCodeImage);
  
  // 5. Вставить QR код
  signaturePage.drawImage(qrImage, {
    x: 50,
    y: 450,
    width: 150,
    height: 150,
  });
  
  signaturePage.drawText(`ID заявки: ${signData.requestId}`, {
    x: 50,
    y: 400,
    size: 10,
  });
  
  signaturePage.drawText('Платформа: 961.kz', {
    x: 50,
    y: 380,
    size: 10,
  });
  
  // 6. Сохранить PDF
  return await pdfDoc.save();
}
```

### API изменения:

#### После успешной верификации кода:
```
POST /api/sign/{token}/verify
Body: { "code": "123456" }

Backend действия:
1. ✅ Проверить код
2. ✅ Изменить статус на "signed"
3. ✅ Генерировать подписанный PDF (оригинал + страница подписи)
4. ✅ Сохранить подписанный PDF
5. ✅ Вернуть ссылку на подписанный документ

Response: {
  "success": true,
  "signedDocumentUrl": "https://cdn.961.kz/signed/REQ-2026-001.pdf"
}
```

### Хранение файлов:

**Оригинальный документ**:
```
/documents/original/{requestId}.pdf
```

**Подписанный документ**:
```
/documents/signed/{requestId}.pdf
```

### Frontend отображение:

#### На странице деталей заявки (`/requests/{id}`):
- **Если статус `pending/viewed`**: показывать **оригинальный PDF**
- **Если статус `signed`**: показывать **подписанный PDF** (с дополнительной страницей)
- **Кнопка "Скачать подписанный документ"**: доступна только если `signed`

#### На странице успеха клиента (`/sign/{token}/success`):
- **Кнопка "Скачать подписанный документ"**: скачивает PDF с подписью

---

## 📌 **Бизнес-логика и валидации**

### Телефоны:
- ✅ Формат: любой, но **строго 11 цифр**
- ✅ Примеры валидных: `+77070000102`, `8 707 000 0102`, `77070000102`
- ✅ Автозамена `8` на `+7` (опционально на бэкенде)

### Заявки:
- ✅ Токен подписания: уникальный, UUID v4
- ✅ SMS-код: 6 цифр, случайный, срок 5 минут
- ✅ Попытки ввода кода: максимум 3, затем блокировка на 10 минут
- ✅ Дедлайн: по умолчанию +7 дней от создания
- ✅ Автоматическое изменение статуса на `expired` при истечении дедлайна (cron job)

### Документы:
- ✅ Формат: только PDF
- ✅ Максимальный размер: 10 МБ (настраивается)
- ✅ Хранение: файловая система или S3-совместимое хранилище

### Аудит:
- ✅ Логирование всех действий пользователей
- ✅ Логирование действий клиентов (просмотр, подписание)
- ✅ Хранение IP-адресов
- ✅ Автоудаление старых логов согласно настройкам (30/90/180/365 дней)

---

## 🔒 **Безопасность**

### Аутентификация:
- ✅ JWT токены
- ✅ Refresh tokens (опционально)
- ✅ Время жизни токена: 24 часа

### Авторизация:
- ✅ Role-based access control (Admin, Manager)
- ✅ Middleware для проверки ролей

### Защита:
- ✅ Rate limiting на API endpoints
- ✅ CORS настройки
- ✅ Валидация всех входящих данных
- ✅ Sanitization для предотвращения XSS/SQL injection
- ✅ HTTPS обязательно в production

### Клиентская часть:
- ✅ Токены подписания одноразовые (после подписания недействительны)
- ✅ Проверка срока действия токена
- ✅ Ограничение попыток ввода SMS-кода
- ✅ Логирование IP-адресов клиентов

---

## 🚀 **Дополнительные фичи для бэкенда**

### Cron Jobs / Scheduled Tasks:
1. **Автоматическое изменение статуса expired**:
   - Каждый час проверять заявки с истекшим дедлайном
   - Менять статус `pending`/`viewed` → `expired`

2. **Напоминания о дедлайнах**:
   - За 24 часа до дедлайна отправлять SMS клиенту (если не подписано)

3. **Очистка старых логов**:
   - Ежедневно удалять логи старше установленного срока

4. **Очистка истекших SMS-кодов**:
   - Каждые 10 минут удалять коды старше 5 минут

### Email уведомления (если включено):
- Новая заявка создана
- Документ подписан
- Заявка отменена
- Истек дедлайн
- Ошибки SMS-провайдера

---

## 📝 **Примеры API запросов**

### 1. Создание заявки
```bash
POST /api/requests
Content-Type: multipart/form-data

clientId: "CL-001"  // или null
clientName: "Иванов Иван Иванович"
clientPhone: "+77070000102"
comment: "Срочный договор"
documentName: "Договор поставки"
documentFile: <PDF file>
```

### 2. Получение статистики Dashboard
```bash
GET /api/dashboard/stats
Authorization: Bearer {token}

Response:
{
  "totalRequests": 156,
  "pending": 23,
  "signed": 98,
  "averageTime": 18.5
}
```

### 3. Проверка токена подписания
```bash
GET /api/sign/abc123xyz789
Response:
{
  "valid": true,
  "request": {
    "id": "REQ-2026-001",
    "clientName": "Иванов Иван",
    "documentName": "Договор поставки",
    "status": "pending",
    "deadline": "2026-02-13T23:59:59Z"
  },
  "documentUrl": "https://cdn.961.kz/documents/abc123.pdf"
}
```

### 4. Отправка SMS-кода
```bash
POST /api/sign/abc123xyz789/send-code
Response:
{
  "success": true,
  "expiresIn": 300
}
```

### 5. Верификация кода
```bash
POST /api/sign/abc123xyz789/verify
Content-Type: application/json

{
  "code": "123456"
}

Response:
{
  "success": true,
  "signedDocumentUrl": "https://cdn.961.kz/signed/abc123.pdf"
}
```

---

## 🎨 **UI/UX особенности**

### Цветовая схема:
- **Primary**: `#0075FF` (синий)
- **Success**: зеленый (подписано)
- **Warning**: желтый (ожидает)
- **Destructive**: красный (отменено)
- **Muted**: серый (истекло)

### Статусы (badges):
- `pending` → желтый, "Ожидает"
- `viewed` → синий, "Просмотрено"
- `signed` → зеленый, "Подписано"
- `expired` → серый, "Истек срок"
- `canceled` → красный, "Отменено"

### Toast уведомления:
- Успешные действия → зеленый toast
- Ошибки → красный toast
- Информация → синий toast

---

## 📦 **Что готово на Frontend**

✅ Все 17 страниц с UI  
✅ Валидация форм  
✅ Фильтры и поиск (готовы к интеграции)  
✅ Toast уведомления  
✅ PDF viewer (react-pdf)  
✅ Адаптивный дизайн (mobile + desktop)  
✅ Дизайн-система (shadcn/ui + Tailwind v4)  
✅ Мок-данные для демонстрации  
✅ TypeScript интерфейсы для всех моделей  

---

## 🔗 **Что нужно от бэкенда**

### Приоритет 1 (MVP):
1. ✅ Авторизация (JWT)
2. ✅ CRUD заявок
3. ✅ CRUD клиентов
4. ✅ Загрузка/просмотр PDF
5. ✅ SMS интеграция (отправка ссылок и кодов)
6. ✅ Клиентская часть (токен → просмотр → код → подпись)
7. ✅ Dashboard статистика
8. ✅ **Генерация подписанного PDF** (оригинал + страница подписи с QR кодом)

### Приоритет 2:
9. ✅ Журнал аудита
10. ✅ Настройки (сохранение/загрузка)
11. ✅ CRUD документов
12. ✅ Управление пользователями

### Приоритет 3:
13. ✅ Cron jobs (expired статусы, напоминания)
14. ✅ Email уведомления
15. ✅ Экспорт CSV (аудит лог)
16. ✅ Повторная отправка SMS

---

## 🎨 **Frontend изменения для подписанных PDF**

### Что уже реализовано:
1. ✅ **PDFViewer компонент** принимает проп `isSigned?: boolean`
2. ✅ **Визуальная индикация** - зелёный баннер сверху PDF viewer'а
3. ✅ **Request Detail Page** - автоматически показывает подписанный PDF если статус = "signed"
4. ✅ **Кнопка скачивания** - меняет текст на "Скачать подписанный PDF" для signed заявок

### Как использовать на фронтенде:
```tsx
// В request-detail-page.tsx
<PDFViewer 
  documentName={request.documentName} 
  onDownload={handleDownload}
  isSigned={request.status === 'signed'}  // ← показывает баннер
/>
```

### Что нужно от бэкенда:
```typescript
// API должен возвращать разные URL для оригинала и подписанного
GET /api/requests/{id}
Response: {
  "request": {...},
  "documentUrl": "https://cdn.961.kz/documents/original/REQ-001.pdf",
  "signedDocumentUrl": "https://cdn.961.kz/documents/signed/REQ-001.pdf" // только если signed
}

// При скачивании:
GET /api/requests/{id}/document  
→ возвращает оригинальный PDF

GET /api/requests/{id}/signed-document  
→ возвращает PDF с добавленной страницей подписи (только если signed)
```

---

## 📧 **Контакты**

Если есть вопросы по фичам или нужны уточнения — готов помочь!

**Frontend готов к подключению API! 🚀**
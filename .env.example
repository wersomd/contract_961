# ============================================
# Contract 961 — Environment Variables
# Copy this file to .env and fill in your values
# ============================================

# ----- Admin -----
# Password for the admin user (used by seed script)
ADMIN_PASSWORD=change-me-strong-password-here

# ----- Database -----
POSTGRES_USER=signdoc
POSTGRES_PASSWORD=change-me-db-password-here
POSTGRES_DB=signdoc_961
DATABASE_URL=postgresql://signdoc:change-me-db-password-here@postgres:5432/signdoc_961?schema=public

# ----- JWT -----
# CHANGE THIS! Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=change-me-jwt-secret-64-chars-minimum
JWT_EXPIRES_IN=7d

# ----- SMSC.kz SMS Provider -----
SMSC_LOGIN=your_smsc_login
SMSC_PASSWORD=your_smsc_password
SMSC_SENDER=961kz

# ----- URLs (update for production) -----
# Frontend URL (where the SPA is served)
FRONTEND_URL=https://contract.961.kz
# Public URL (backend public endpoint for QR codes, verification links)
PUBLIC_URL=https://contract.961.kz

# ----- AWS S3 (optional, for file storage) -----
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=eu-central-1
# AWS_S3_BUCKET=contract-961-documents

